{
  "name": "chipandchap",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal single-screen Phaser 3 platformer scaffold.",
  "scripts": {
    "dev": "vite",
    "build:web": "vite build",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
